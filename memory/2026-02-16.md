# 2026-02-16 Session Notes

## User
- **cmshoff** (Discord), owner of Chores-Tracking-and-Accounting project
- Runs on Raspberry Pi kiosks (3 prototypes: Pi 4B original, Pi 3B new, Pi 4B high-res LCD)
- Deploy user: `cmshoff`, deploy path: `~/chores_repo/scripts/deploy_release.sh`
- Data dir: `/var/lib/chores_app/`, app dir: `~/chores_app/current` (symlink to releases)

## Work Completed

### Code Review Bug Fixes (16/16 issues resolved)
- #1 PIN hashing (bcrypt→PBKDF2 stdlib after bcrypt caused 5min compile on Pi)
- #2 Debug reset PIN-protected
- #3 CORS locked to localhost
- #4 datetime.utcnow() replaced
- #5 **CRITICAL** Double-credit bug fixed (management.py no longer credits balance directly)
- #6 await check_missed_payouts
- #7 Approval systems consolidated (management delegates to approvals)
- #8 ISO week numbering (%G-W%V)
- #9 Global TZ mutation removed
- #10-16 Various quality fixes, Pydantic models, tests, etc.

### Deploy Script Fixes
- Font install skip if already present
- Cleanup runs before reboot (was dead code)
- Variable naming fixed
- Deploy output now logs to `~/chores_app/deploy.log`

### Feature Batch
- **Late approval retroactive payout** — catch-up credit when approving after weekly tally
- **Avatars** — 8 sci-fi SVG presets + initials fallback, picker in Manage Crew
- **Sounds** — 7 auto-generated WAV effects wired to key actions
- **Keyboard symbols** — !@# toggle with full special char layout
- **Dynamic kid cards** — up to 10 kids, 6 responsive tiers
- **XP weight removed** from UI (unused by payout logic)
- **Screensaver popup fix** — closes dialogs before entering screensaver

### Rotation Chores Feature (Major)
- 3 new tables: RotationGroup, RotationMember, RotationLog
- 3 frequencies: ALTERNATING_DAILY, EVERY_OTHER_DAY, BIWEEKLY
- Deterministic assignment: `days_since_start % len(members)`
- Full CRUD API, integrated into payout, automation, approvals
- Kiosk UI: dashboard shows shared quests, quest manager has rotation creator
- Alembic migration included

## Deployment Issues Encountered
- bcrypt native compilation took 5+ min on Pi, hung deploy → switched to stdlib PBKDF2
- `~/chores_repo` is separate from deployed releases — must `git pull` there before kiosk update button works with new deploy script
- Pi 4B (prototype 1) had zombie deploy process — needed manual symlink switch + reboot
- Deploy script sends output to /dev/null (now fixed to log file)
- ~100 DB backup files accumulated (not a storage issue, ~8MB total)
- `set -e` in deploy means any failure kills script before reboot

## Key Architecture Notes
- Kiosk = PySide6 on bare X11, backend = FastAPI + SQLite + SQLModel
- Payout modes: ALL_OR_NOTHING (threshold%) or PRORATED (instances/expected * allowance)
- Per-chore `reward` field exists but is NOT used by payout calculation
- `alembic upgrade head` runs during deploy (step 7)
- SystemD services: chores-backend, chores-kiosk

## User Preferences
- Wants API cost tracking per interaction
- Prefers working through lists systematically
- Tests updates live on prototypes via kiosk "Check for Updates" button
- Wants specs before building large features
