# 2026-03-11

## Session Summary
Continuing Quest2Earn chores kiosk project. All major work complete through commit `2ccdd93`.

### Completed Today (carried from session)
- All 16 code review issues fixed
- Feature batch: late approval retroactive payout, screensaver, avatars, keyboard, dynamic kid cards, sound effects
- Alternating/biweekly chores (rotation groups) — full stack including kiosk + admin portal
- Admin portal 1:1 parity rewrite with avatar picker, rotations tab, rotation-aware approvals
- Progress calculation bug fix (week_id format mismatch → date range queries + startup migration)
- Quick Start Guide PDF generated (`docs/Quest-Tracker-Quick-Start-Guide.pdf`)
- Approval queue UI freeze bug fix (missing HTTP timeouts blocking Qt event loop)

### Status
- Latest commit: `2ccdd93` on main
- Next: verify all 3 Pi prototypes running latest, confirm payout mode, UI polish
- Estimated API cost so far: ~$3.50-4.00

### Key Technical Notes
- bcrypt replaced with PBKDF2 (bcrypt caused 5+ min deploy hangs on Pi)
- `calculate_weekly_progress` uses date range queries not week_id strings
- `fix_week_id_format()` runs at startup to fix legacy ChoreLog records
- All kiosk HTTP calls now have `timeout=5`
- Deploy via `~/chores_repo/scripts/deploy_release.sh` (must `git pull` chores_repo first)
